%h1 Listing orders

%table
  %thead
    %tr
      %th Date
      %th First course dish
      %th Second course dish
      %th Drink
      %th User
      %th Menu
      %th
      %th
      %th

  %tbody
    - @orders.each do |order|
      %tr
        %td= order.date
        %td= order.first_course_dish_id
        %td= order.second_course_dish_id
        %td= order.drink_id
        %td= order.user_id
        %td= order.menu_id
        %td= link_to 'Show', order
        %td= link_to 'Edit', edit_order_path(order)
        %td= link_to 'Destroy', order, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Order', new_order_path
